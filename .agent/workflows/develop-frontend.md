---
description: Implement a frontend Jira ticket following an implementation plan
---

1. Analyze the Jira ticket and Figma URL in $ARGUMENTS ($1: Ticket ID, $2: Figma URL).
2. Generate a components tree (atoms -> molecules -> organisms -> page).
3. Start a new branch `feature/[ticket-id]-frontend`.
4. Write React components and styles following the design.
5. Use React Bootstrap and existing UI components where possible.
6. Write Cypress E2E tests for the new functionality.
7. Update relevant documentation and create a PR.
